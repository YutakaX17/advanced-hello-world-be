# Security

Report vulnerabilities through GitHub private vulnerability reporting, never a
public issue. Do not commit secrets. Production deployments must use restricted
hosts, secure cookies behind TLS, and a non-superuser database account.

GitHub secret scanning and push protection are enabled. Rotate an exposed
credential before cleaning history or closing an alert.
