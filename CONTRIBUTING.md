# Contributing

Create a short-lived `feature/`, `fix/`, `docs/`, or `chore/` branch and open a
pull request into `main`. Use Conventional Commits. Document configuration,
migration, container, and core-compatibility impact and require green checks.

